

describe('ChoreService', () => {


  const prefix = 'TM1ts_test_';
  const choreName = prefix + 'chore';

  const setup = async () => {}
  const cleanup = async () => {}

  beforeAll(async () => {
    await setup()
  })

  afterAll(async () => {
    await cleanup()
  })

})